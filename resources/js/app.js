import './bootstrap';
import 'flowbite';
import 'tw-elements';
import {
    Input, Ripple, Modal,
    Carousel,
    initTE } from "tw-elements";

// initialising tw-elements
initTE({ Input, Ripple });
initTE({ Carousel });

initTE({ Modal });
